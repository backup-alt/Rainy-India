name: News Cameraman (Self-Hosted Windows)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'How long to watch? (seconds)'
        default: '60'

jobs:
  watch-news:
    runs-on: self-hosted
    timeout-minutes: 360

    steps:
    - name: Get Stream & Capture
      shell: powershell
      run: |
        Write-Host "--- Debugging Tools ---"
        python --version
        
        Write-Host "--- Finding Thanthi TV Stream ---"
        
        # FIX: Added '--no-warnings' so PowerShell doesn't panic at text output
        # We also added '2>$null' to force-silence any other non-critical errors
        $StreamUrl = python -m yt_dlp -f best -g "https://www.youtube.com/@ThanthiTV/live" --no-warnings 2>$null
        
        # Check if we got a link
        if ($StreamUrl -match "^http") {
             Write-Host "Found Stream URL!"
             Write-Host $StreamUrl
             
             Write-Host "--- Starting Capture ---"
             ffmpeg -i "$StreamUrl" -vf "fps=0.4,scale=854:480,crop=854:120:0:360" -t ${{ inputs.duration }} -q:v 5 screenshot_%04d.jpg
        } else {
             Write-Error "FAILED to get Stream URL. The variable was empty."
             exit 1
        }

    - name: Upload Evidence
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: thanthi-tv-evidence
        path: "*.jpg"
